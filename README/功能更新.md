# 功能更新

## 2026-08-16：suggests / 级联建议

- **级联建议解析**：`RelationshipResolver::resolve` 新增 `withSuggests` 参数，开启后把安装集（含依赖）中各模组的 `suggests` 建议模组收集到 `ResolutionResult::suggestedModules`（仅收集、不自动安装），并对建议模组的建议继续级联展开；已安装/已选中的模组不会作为建议出现，级联去重防成环。实现于 `relationshipresolver.h/.cpp`。
- **安装前勾选弹窗**：`CKanManager::resolveAndInstall` 在依赖解析后，若有建议模组则弹出勾选对话框（每个建议显示名称、标识符、版本、简介，默认全勾选，可滚动查看），用户「安装所选」后把选中的模组并入安装集重新解析（连其依赖一起下载）；「取消」则中止本次安装。实现于 `ckanmanager.h/.cpp` 的 `askSuggests`。
- **开关设置**：`ConfigManager` 新增 `installSuggests`（默认开启，持久化到 `HKSPL.json`），设置页「模组管理」新增「安装时显示建议模组」开关（`ToggleSwitch`，`settingspage.h/.cpp`）。
- **单测**：`TestRelationshipResolver` 新增 6 个用例——`suggestsCollectedWhenEnabled`（建议进集合但不自动安装）、`suggestsIgnoredWhenDisabled`（关闭时不收集）、`suggestsCascade`（级联收集 B/C）、`suggestsExcludeInstalled`（已安装不再建议）、`suggestsCycleTerminates`（建议成环去重）、`suggestsSelectedThenResolvedWithDeps`（弹窗选中后重新解析，选中模组的依赖一并进入安装集）（`tests/test_libckan.cpp`），全部通过。
- 涉及文件：`src/ckan/relationshipresolver.h/.cpp`、`src/ckan/ckan.h/.cpp`、`src/ckanmanager.h/.cpp`、`src/configmanager.h/.cpp`、`src/pages/settingspage.h/.cpp`、`tests/test_libckan.cpp`。

## 2026-08-16：build ID → 版本映射

- **build ID → 版本映射**：`GameInstance::detectVersion()` 优先读取实例根目录的 `buildID64.txt` / `buildID.txt`，解析官方格式 `build id = NNNN`（忽略前导 0，也兼容整个文件就是纯数字），经内置静态映射表（数据源自官方 CKAN 的 builds-ksp.json，覆盖 0.23.0.395 ~ 1.12.5.3190 全量正式版）换算成语义版本；两个文件都读，去重后取版本最大值（对齐官方 `KspBuildIdVersionProvider`）。
- **兜底**：buildID 缺失或 id 未命中映射表时，回退解析 `readme.txt` 中的版本行；都失败返回无效版本。实现于 `gameinstance.cpp` 的 `kBuildMap()` / `buildIdToVersion()`。
- **单测**：`TestGameInstance` 新增 5 个用例——行格式解析（含前导 0）、纯数字格式、双文件取最大值、id 未命中回退 readme、无任何信息返回无效（`tests/test_libckan.cpp`），全部通过。
- 涉及文件：`src/ckan/gameinstance.h/.cpp`、`tests/test_libckan.cpp`。

## 2026-08-16：下载 SHA256 校验 + 多线程下载 + 修复忽略实际 KSP 版本

- **下载 SHA256 校验**：模组下载内容校验器除「必须为 ZIP 归档」外，若仓库元数据声明了 `download_hash` 的 sha256，则对下载内容计算 SHA256 比对，不一致判定下载失败并自动重试镜像，且失败结果不写入缓存。实现于 `ModuleInstaller::downloadOneTask` 的 `zipValidator`（`moduleinstaller.cpp`）。
- **多线程并行下载**：`ModuleInstaller::downloadModules` 改为 `QtConcurrent::run` + `QThreadPool` 并行下载多个模组，保留原有断点续传、取消与聚合进度（跨线程 `std::atomic<qint64>` 累计字节数）；并发数默认 3，范围 1~8，新增 `ConfigManager::downloadConcurrency` 持久化到 `HKSPL.json`，设置页「模组管理」新增「下载并发数」下拉框（`settingspage.h/.cpp`）。`libckan` 链接新增 `Qt6::Concurrent`。
- **修复完全忽略实际 KSP 版本的问题**：此前兼容性检查用固定中版本门槛（minor ≥ 9），与游戏实际版本无关。现按官方 CKAN 实现——新增 `GameVersionRange`（含 contains/intersects，对应 CKAN-master GameVersionRange.cs），`CkanModule::isCompatible(ksp)` 基于 `ksp_version`/`ksp_version_min`/`ksp_version_max`/`ksp_version_strict` 构造区间并与实际检测到的游戏版本比较（`version.h/.cpp`、`ckanmodule.cpp`）；`ModsTableModel` 新增 `setGameVersion()`，实例详情页把实例实际 KSP 版本传入过滤代理模型（`modtablemodel.h/.cpp`、`instancedetailpage.cpp`、`ckanmanager.cpp` 的 `detectedVersion`），不兼容过滤随之按真实版本判定。
- **单测**：新增 `TestModuleDownload`（哈希匹配通过/哈希不匹配拒绝且不写缓存/并发下载 2 个模组均落盘）、`TestGameVersion` 的 `rangeContains` 区间判定（`tests/test_libckan.cpp`）。
- 涉及文件：`src/ckan/version.h/.cpp`、`src/ckan/ckanmodule.cpp`、`src/ckan/moduleinstaller.h/.cpp`、`src/ckanmanager.h/.cpp`、`src/pages/modtablemodel.h/.cpp`、`src/pages/instancedetailpage.cpp`、`src/configmanager.h/.cpp`、`src/pages/settingspage.h/.cpp`、`CMakeLists.txt`、`tests/test_libckan.cpp`。

## 2026-08-16：修复模组列表显示旧版本而非最新版本

- **问题**：同一模组（如 Kerbal Konstructs）在启动器模组列表中显示为 `0.5.1b`，而官方 CKAN 显示并安装 `v1.12.2.0`。
- **根因**：`CKan::search()` 用 `versions.first()` 取每个模组的代表版本，但 `m_index` 中同一标识符的各版本是按 CKAN-meta tar 包内文件名**字母序**存放的，从未按版本排序；`versions.first()` 实际是「字母序最小」的版本（Kerbal Konstructs 即 `0.5.1b`）。而官方 CKAN 用版本字典升序后取首个兼容版本，故为 `v1.12.2.0`。启动器内部安装/升级路径用的 `latestOf()`（`RepoIndex::latestFor`，已按版本排序）是正确的，与列表显示不一致。
- **附带后果**：`0.5.1b` 的 `ksp_version` 为 `0.25`（minor < 9），按 `isCompatible` 判定不兼容，默认「显示不兼容」关闭时该模组会被整个隐藏。
- **修复**：`search()` 改为调用 `RepoIndex::latestFor(m_index, it.key())` 按版本取最新，列表显示、兼容性过滤、实际安装三者统一。
- **测试**：新增 `TestRepoIndex` 套件（`latestPicksNewestNotAlphabeticalFirst` 回归用例：0.5.1b / 1.8.1.15 / v1.12.2.0 乱序下必须取到 v1.12.2.0；`latestEmptyIndex` 空索引用例），全部通过。
- 涉及文件：`src/ckan/ckan.cpp`、`tests/test_libckan.cpp`。

## 2026-08-16：设置页增加滚动区域

- 设置页内容在默认窗口（1000×650）下会超出可视区，导致下部的「模组管理」下拉框被窗口底部裁剪、看不见文字；拉大窗口后才正常。
- 修复：将设置页整体内容包进 `QScrollArea`（`setWidgetResizable(true)`、隐藏横向滚动条），内容超出时可垂直滚动查看，下拉框不再被裁剪且保持自动拉伸填满宽度。
- 涉及文件：`src/pages/settingspage.h/.cpp`。

## 2026-08-16：修复设置页模组管理下拉框过窄

- 设置页「模组管理」区域的三个下拉框（索引刷新间隔、索引下载源、模组下载源）因 QSS 中 `min-width: 210px` 固定宽度导致在宽窗口下显得过窄。
- 修复：移除 `dark.qss` / `light.qss` 中 `QComboBox` 的 `min-width`，改为在代码中通过 `setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Fixed)` 让下拉框随 QFormLayout 自动拉伸填满可用宽度。
- 涉及文件：`src/pages/settingspage.cpp`、`resources/themes/dark.qss`、`resources/themes/light.qss`。

## 2026-08-16：设置新增模组管理项

- **索引刷新间隔**：设置页新增「索引刷新间隔」下拉（6小时/1天/3天/5天），控制仓库索引缓存有效期。默认 6 小时；到期的缓存自动重新下载，未到期直接使用本地缓存。实现：`ConfigManager::indexRefreshIntervalSecs`，`RepoIndex::buildCached(maxAgeSecs)`，`CKanManager::refreshIndexAsync` 从配置读取。
- **索引下载源**：下拉可选「尽量选择官方源 / 尽量选择镜像源」，默认官方源。选镜像源时索引下载优先走 GitHub 代理镜像，失败回退官方。实现：`Downloader` 增加 `preferMirror`，`RepoIndex::build/buildCached` 与 `CKan::refreshIndex` 透传。
- **模组下载源**：下拉可选「尽量选择官方源 / 尽量选择镜像源」，默认官方源。选镜像源时模组 zip 优先拼接镜像前缀下载，失败回退官方。实现：`ModuleInstaller::install` 新增 `mirrorPrefixes`/`preferModuleMirrors`，`CKan::install` 透传。
- **下载缓存文件夹**：可点击「选择文件夹」自选 zip 缓存目录（默认 `exe目录/ckan_cache/downloads`），选择后可选择是否把旧目录缓存的 zip 迁移到新目录；新目录持久化到 `HKSPL.json` 的 `downloadCacheDir`。
- **清理缓存**：「清理缓存」按钮仅精确删除与已知模组对应的缓存 zip（按索引全部版本 + 已安装模组，以 `标识符_安全版本.zip` 精确匹配文件名），目录中其他文件一律保留，绝不误删。实现：`CKanManager::cleanDownloadCache`（依赖 `CKan::allIdentifiers`）。
- 涉及文件：`src/configmanager.h/.cpp`、`src/ckan/downloader.h/.cpp`、`src/ckan/repoindex.h/.cpp`、`src/ckan/ckan.h/.cpp`、`src/ckan/moduleinstaller.h/.cpp`、`src/ckanmanager.h/.cpp`、`src/pages/settingspage.h/.cpp`。

## 2026-08-16：手动模组文件夹冲突处理 + AD 依赖不重复下载

- **手动模组识别**：扫描 `GameData` 下未被注册表登记的任何顶层文件夹（排除 `Squad`/`SquadExpansion` 官方目录），识别为手动安装占用的文件夹（含无 DLL 的纯配置/纹理模组）。实现：`GameInstance::manualGameDataFolders()`（`gameinstance.h/.cpp`）。
- **AD 依赖不重复下载**：依赖解析时，把手动安装（DLL 扫描，AD）模组的标识符加入「已提供」集合——若待装模组依赖一个 AD 模组，直接视为已满足，不再下载也不再报缺失。实现：`relationshipresolver.cpp`。
- **两阶段安装 + 安装前冲突检测**：安装/升级拆分为「下载全部 zip」→「读取 zip 实际内容推导将写入的 `GameData` 顶层文件夹并检测冲突」→「（UI 弹窗）解决冲突」→「从缓存安装」。以 zip 真实内容为准（`ModuleInstaller::actualGameDataFolders`，套用 `as`/`find`/`file` 与嵌套 `install_to` 规则），全覆盖 `GameData/<模组作者>/模组名` 这类结构，绝不依赖预估；检测时间在下载后、写入前，边装边解决。
- **冲突弹窗（3 选项）**：有冲突时弹窗让用户选择：
  - **全部覆盖**：保留旧的额外文件，直接写入新模组文件；
  - **全部删除旧的保留新的**：先递归删除冲突的旧文件夹再安装（干净安装）；
  - **取消**：中止本次安装。
- **删除旧文件夹**：`ModuleInstaller::installFromCache` 的 `foldersToDelete` 参数对命中的顶层文件夹递归删除（`removeDirRecursively`）。
- **涉及文件**：`src/ckan/gameinstance.h/.cpp`、`src/ckan/moduleinstaller.h/.cpp`、`src/ckan/relationshipresolver.cpp`、`src/ckanmanager.h/.cpp`、`src/pages/instancedetailpage.cpp`（安装阶段进度条不确定态）。
- **单测**：新增 `adDependencySatisfied`/`nonAdDependencyStillDownloaded`（AD 依赖跳过 vs 普通依赖仍下载）、`actualGameDataFolders`（默认/as/嵌套/多文件夹/非 GameData）、`manualGameDataFoldersDetected`（手动占用识别）、`installDeletesOldFolder`（删除旧文件夹后再安装）（`tests/test_libckan.cpp`）。

## 2026-08-15：模组管理 UI 美化与按钮合并

- **统一行透明**：模组表格移除隔行变色，所有行背景统一为透明，仅选中行高亮、悬停轻微加深，观感与侧边栏一致。
- **半透明面板**：模组管理区整体加一个与侧边栏同风格的半透明底（`#modsContentWidget`），亮色 `rgba(243,243,243,204)`、暗色 `rgba(37,37,38,165)`，背景图透出更统一。
- **操作按钮合并**：将「批量安装 / 批量升级 / 安装 / 升级」合并为「安装」「升级」两个按钮，并保留「卸载」；通过勾选数量自动判定走单个还是批量（勾选 ≥2 走批量且按钮文案带数量，只勾 1 个走单个）。
- 涉及文件：`src/pages/instancedetailpage.h/.cpp`、`resources/themes/dark.qss`、`resources/themes/light.qss`。

## 2026-08-14：补全英语本地化

- 用 `lupdate` 全量抽取源码中的可翻译字符串，补齐 `hello_ksp_launcher_en_US.ts` 中缺失的 78 条英文翻译（覆盖级联卸载、批量安装/升级/卸载、仓库索引加载/刷新、下载进度与取消、KSP 版本兼容等近期新增功能）。
- 所有条目均完成翻译，`unfinished` 展位清零；`lrelease` 重新编译后 `hello_ksp_launcher_en_US.qm` 生成 253 条已译文本（253 finished / 0 unfinished）。
- 涉及文件：`translations/hello_ksp_launcher_en_US.ts`（仅翻译，未改动源码逻辑）。

## 2026-08-14：卸载时级联卸载依赖本模组的模组

- **行为**：卸载一个模组时，会自动查找所有**直接或间接**依赖它的已安装模组，按依赖链自外向内一并卸载（如 C 依赖 B、B 依赖 A，卸载 A 时顺序为 C、B、A），最后卸载目标模组本身；没有依赖它的模组时仅卸载自身。
- **实现**：`ModuleInstaller::uninstall` 改为级联卸载，新增 `collectReverseDeps`（递归收集反向依赖链，含虚拟包匹配，`visited` 防循环依赖），卸载顺序为后序（先依赖者、最后目标）（`src/ckan/moduleinstaller.cpp`）。
- **提示**：级联卸载会在卸载完成消息中列出全部被卸载的模组（`CKanManager::uninstallAsync`）。
- **单测**：`TestGameInstance` 新增 `uninstallCascade`（验证 C/B/A 顺序卸载、无关模组保留）与 `uninstallNoDependentsOnlyTarget`（无依赖者时仅卸自身）（`tests/test_libckan.cpp`）。

## 2026-08-14：修复带 epoch 的模组缓存文件 0 字节问题

- **问题**：安装带 epoch 版本的模组（如 Community Tech Tree 的 `version = 1:3.4.0`）时，缓存文件名拼接为 `标识符_1:3.4.0.zip`。Windows/NTFS 把文件名中的冒号 `:` 当作 ADS（备用数据流 `:3.4.0.zip`）分隔符，数据写入不可见的流，主文件流为 0 字节，导致下载完成后报 `corrupt zip ... size=0 bytes`。
- **修复**：新增 `ModuleInstaller::safeCacheFileName()`，对缓存文件名中的非法字符（`: \ / ? * < > | "`）统一替换为 `_`，`1:3.4.0` → `1_3.4.0`（`src/ckan/moduleinstaller.cpp/.h`）。
- **单测**：新增 `TestModuleInstallDescriptor::safeCacheFileName`，覆盖 epoch 冒号替换、普通版本号保留、其余非法字符清洗（`tests/test_libckan.cpp`）。

## 2026-08-14：GameData DLL 扫描 + KSP 版本兼容检查

- **手动模组识别（AD）**：打开实例时扫描 `GameData` 下所有 `.dll`（排除 `Squad`、`SquadExpansion` 官方目录），以 DLL 文件名第一个 `.` 之前的部分作为标识符，识别手动安装的模组。
  - 实现：`GameInstance::scanUnmanagedDlls()`（`gameinstance.h/.cpp`），`CKanManager::scanUnmanagedDlls/isAutoDetected`。
- **AD 状态展示**：手动安装模组在表格中状态显示为 `AD`，复选框禁用，安装/卸载/升级按钮均不可用；状态过滤时归入「已安装」分类（选择「已安装」时 AD 模组同样显示）（`modtablemodel`、`instancedetailpage.cpp` 的 `updateModActionButtons`）。
- **KSP 版本兼容检查**：兼容门槛固定为中版本规则——模组 `ksp_version` 的中版本 ≥ 9（即 1.9.0）视为兼容，< 9 不兼容；未声明或非法 `ksp_version` 视为兼容。
  - 实现：`CkanModule::isCompatible()`（`ckanmodule.cpp`，常量 `kCompatibleMinMinor = 9`）。
- **不兼容模组过滤**：不兼容模组默认隐藏，模组管理页顶栏新增「显示不兼容」开关，勾选状态持久化到 `HKSPL.json` 的 `showIncompatibleMods`（`ConfigManager`、`ModsFilterProxyModel::filterAcceptsRow`）。
- **单测**：新增 `TestGameInstance`（官方目录排除、多文件去重、无 GameData 返回空），重写 `TestCkanModule::compatible` 以匹配固定中版本门槛规则（`tests/test_libckan.cpp`）。

## 2026-08-14：registry.json 补全必填字段（官方 CKAN 兼容）

- 用官方 CKAN 打开实例时报「缺少必填字段 license」，根因是 `CkanModule::toJsonObject()` 只序列化了 6 个字段，落到 `registry.json` 的 `source_module` 里缺少 `license` 等官方必填字段。
- 将 `CkanModule::toJsonObject()` 改为与 `fromJsonObject()` 完整对应的往返序列化，补齐 `license`、`author`、`ksp_version`、`download`、`download_hash`、`resources`、依赖关系、安装规则等全部元数据，`provides` 按 CKAN 原格式输出为标识符字符串列表。
- 新增 `Relationship::toJsonObject()`（还原 name + version 约束）与 `ModuleInstallDescriptor::toJsonObject()`，供上述序列化使用。
- 涉及文件：`src/ckan/ckanmodule.cpp`、`src/ckan/relationship.h/.cpp`、`src/ckan/moduleinstalldescriptor.h/.cpp`
- 新增 `TestRegistry::licenseRoundtrip` 单测：验证注册表序列化后 license/author/ksp_version 完整保留（`tests/test_libckan.cpp`）

## 2026-08-14：模组批量安装 / 升级 / 卸载（复选框）

- **复选框列**：模组表格首列新增复选框，勾选状态按标识符记忆，跨搜索/筛选保留；点行看详情与勾选互不影响。
- **全选/清空**：顶栏新增「全选/清空」按钮，一键切换当前可见模组的勾选。
- **批量操作**：底部新增「批量安装 / 批量升级 / 批量卸载」三个按钮。
  - 批量安装：解析所有勾选未安装模组的依赖集合（整体解析，冲突/缺失依赖则整体失败），跳过已安装项。
  - 批量升级：仅处理可升级项，先卸载旧版再安装新版。
  - 批量卸载：仅处理已安装项，逐个卸载。
- **进度**：复用现有批量进度条（按批量总字节聚合），取消/超时机制同样生效。
- **接入面**
  - `ModsTableModel`：新增 `ColCheck` 列、`setData/flags`、勾选记忆与 `checkedIdentifiers/clearAllChecks`（`modtablemodel.h/.cpp`）
  - `CKan::resolveInstallMany`：一次性解析多模块安装集（`ckan.h/.cpp`）
  - `CKanManager`：`installBatchAsync / upgradeBatchAsync / uninstallBatchAsync`，`runInstall` 支持前置卸载（`ckanmanager.h/.cpp`）
  - `InstanceDetailPage`：全选按钮 + 三个批量按钮，`setModButtonsEnabled` 统一启停（`instancedetailpage.h/.cpp`）
- 新增 `TestRelationshipResolver::unionSharedDependency`：验证批量安装时共享依赖只安装一份（`tests/test_libckan.cpp`）

## 2026-08-14：索引获取与模组下载的进度条 / 超时 / 取消

- **进度条**：模组下载与仓库索引获取均接入进度条（位于模组管理页下载框区域）。模组按批量总字节数聚合进度，并显示已下载大小与实时速度；索引 tar.gz 因总量未知，进度条呈不确定态。
- **超时机制**：HTTP 连接建立与传输空闲均由 `setTransferTimeout(30s)` 统一兜底（Qt 的 transferTimeout 每次收包都会重置，等同 30s 连接 + 30s 空闲超时）。
- **取消支持**：下载线程通过 `std::atomic_bool` 轮询取消标志，用户点「取消」即中止当前下载；取消索引或模组下载均不弹错误框，已下载的临时数据不写入缓存。
- **接入面**
  - `Downloader::downloadProgressed`：新增进度回调 / 超时 / 取消（`downloader.h/.cpp`）
  - `ModuleInstaller::install`：聚合批量总进度，`byteProgress` 信号携带已下载字节 / 总量 / 速度（`moduleinstaller.h/.cpp`）
  - `RepoIndex::build/buildCached` 与 `CKan::refreshIndex`：透传进度回调与取消标志（`repoindex.h/.cpp`、`ckan.h/.cpp`）
  - `CKanManager`：冒泡 `downloadProgress` 信号，`cancelCurrentOperation()` 同时取消索引与安装（`ckanmanager.h/.cpp`）
  - `InstanceDetailPage`：进度条 + 大小/速度标签 + 取消按钮，索引刷新与安装/升级共用（`instancedetailpage.h/.cpp`）
- 新增 `TestDownloader` 单测：本地文件验证进度回调与取消标志路径（`tests/test_libckan.cpp`）

## 2026-08-14：CKAN 下载健壮性 + 仓库索引缓存

- **下载健壮性**：下载器支持内容校验（`Downloader::Validator`），模块下载会校验是否为 ZIP 归档，非预期内容（错误页/被截断）视为失败并自动重试镜像；下载前校验缓存有效性，损坏缓存自动删除重下；错误信息更明确（区分网络错误与内容校验失败）。zip 打开改用内存读取（`mz_zip_reader_init_mem`），规避 Windows fopen 的 ANSI 路径编码问题。
- **仓库索引缓存**：新增 `RepoIndex::setCacheDir` / `buildCached`，将 CKAN-meta 的 tar.gz 落盘到 `exe目录/ckan_cache/index/`，默认有效期 6 小时。进入模组页自动加载时优先用缓存（不再每次启动都下载）；点「刷新仓库」则强制重新下载（`refreshIndexAsync(true)`）。
- 涉及文件：`downloader.h/.cpp`、`moduleinstaller.cpp`、`repoindex.h/.cpp`、`ckan.h/.cpp`、`ckanmanager.h/.cpp`、`pages/instancedetailpage.cpp`

## 2026-08-14：内置 CKAN 模组管理（libckan）

- 新增 `libckan` 静态库（`src/ckan/`），用 C++ 重写 CKAN 核心，数据库（CKAN-meta 仓库）与 registry.json 格式完全兼容官方版本
- 保留独立接口（`ckan::CKan` 等类），供启动器后续接入更多功能

### 核心逻辑

- `version`：`ModuleVersion` / `GameVersion`，实现与官方 CKAN 一致的语义化版本比较（含 epoch、build 忽略规则）
- `ckanmodule`：模块元数据 `CkanModule` 的 JSON 解析/序列化，缺失 identifier 或 version 判定为无效
- `relationship`：依赖/推荐/冲突/提供关系描述符及版本约束判定
- `moduleinstalldescriptor`：安装规则（file/find/find_regexp）解析、文件筛选与路径转换（GameData/GameRoot/Ships 等）
- `registry`：registry.json 读写，`installed_modules` / `installed_files` / `sorted_repositories` 完整兼容
- `relationshipresolver`：BFS 依赖展开，支持 depends/recommends/conflicts/provides，虚拟包索引解析，自动选择满足约束的最高版本
- `repoindex`：解析 CKAN-meta tar.gz，构建 标识符→多版本 仓库索引
- `downloader` / `moduleinstaller`：HTTP 下载（含镜像/代理）、miniz 解压、安装/卸载与文件归属跟踪
- `gameinstance` / `ckan`：实例生命周期封装与对外统一入口

### 界面与接入

- `CKanManager` 单例（`src/ckanmanager`）：封装 libckan，提供异步刷新索引/安装/卸载/升级，供 UI 调用
- 实例管理页「模组」标签页重构为完整模组管理界面：搜索、状态筛选、详情、安装/卸载/升级，`QTableView`+`ModsTableModel`+过滤代理模型
- 所有新文案均已 `tr()` 包裹，纳入 zh_CN / en_US 双语翻译

### 单元测试

- 新增 `tests/test_libckan.cpp`（QtTest），覆盖版本比较、安装规则、registry、依赖解析、虚拟包、冲突、缺失依赖等 44 个用例
- `CMakeLists.txt` 新增 `test_libckan` 目标，`enable_testing()` + `add_test` 接入 CTest

### 相关文件变更

- 新增 `src/ckan/*`（13个源文件）、`src/ckanmanager.h/.cpp`、`src/pages/modtablemodel.h/.cpp`、`tests/test_libckan.cpp`
- 修改 `src/pages/instancedetailpage.h/.cpp`（模组标签页）
- 修改 `CMakeLists.txt`（libckan、test_libckan、Qt Test 组件）

## 2026-08-14：实例管理「浏览」菜单

- 在实例管理侧边栏「导出整合包」下方新增「浏览」按钮（folder-open 图标）
- 点击后从按钮下方弹出下拉菜单，包含以下浏览目标：
  - 游戏根目录（根目录）
  - KSP.log（根目录/KSP.log，文件）
  - 模块日志（根目录/Logs）
  - Principia日志文件夹（根目录/glog/Principia）
  - 模组文件夹（根目录/GameData）
  - 飞船文件夹（根目录/Ships）
  - kOS代码文件夹（根目录/Ships/Scripts，存在时才可用）
  - 存档文件夹（根目录/saves）
- 目标不存在时对应菜单项置灰禁用
- 文件用系统默认应用打开，文件夹在系统文件管理器中打开（QDesktopServices::openUrl，跨平台）
- 新文案已补充 zh_CN / en_US 双语翻译

### 相关文件变更

- `src/pages/instancedetailpage.h/.cpp`：新增浏览按钮、下拉菜单及打开逻辑
- `translations/hello_ksp_launcher_zh_CN.ts` / `hello_ksp_launcher_en_US.ts`：新增 13 条文案并重新编译 .qm


## 2026-08-12：半透明 UI 与自定义背景图

- 整体 UI 改为半透明风格：边栏、启动栏、内容卡片（实例项/设置分组/列表等）均使用 rgba 半透明背景
- 边栏透明度约 65%，卡片透明度约 75%，输入框与按钮保持不透明以保证可读性
- 主窗口底层新增 QLabel 背景层，显示当前背景图（Cover 模式：保持比例填充，超出裁剪）
- 跟随窗口缩放自动重绘背景图
- 浅色/深色主题均支持半透明与背景图

### 背景图片管理

- 新增 `BackgroundManager` 单例（src/backgroundmanager），负责加载、缓存、切换背景
- 默认背景打包在资源中：`resources/backgrounds/default.png`（来自 `Kerbal Space Program BG2.png`）
- 用户可在「启动器设置 → 背景图片」中选择本地图片替换背景，支持 PNG / JPG / JPEG
- 用户选中的图片会被复制到 `<appDir>/backgrounds/user_bg_<uuid>.<ext>`，避免原文件被移动/删除后失效
- 设置项 `backgroundPath` 持久化到 HKSPL.json，下次启动自动恢复
- 提供"重置为默认"按钮，一键回到默认背景
- 设置面板含 140px 高度缩略图预览 + 当前路径显示

### 相关文件变更

- `src/backgroundmanager.h/.cpp` 新增
- `src/configmanager.h/.cpp` 新增 `backgroundPath` 字段及 getter/setter
- `src/mainwindow.h/.cpp` 新增 `m_backgroundLabel`/`m_contentContainer`，重写 `resizeEvent`/`changeEvent`
- `src/pages/settingspage.h/.cpp` 新增「背景图片」分组 UI
- `src/pages/homepage.cpp` 改用 `state` 动态属性 + QSS 状态选择器，避免 setStyleSheet 覆盖主题
- `resources/themes/dark.qss` / `light.qss` 全面重写为半透明
- `resources/resources.qrc` 引入 `backgrounds/default.png`
- `CMakeLists.txt` 编译新源文件

## 2026-08-12：导出整合包功能

- 在实例管理侧边栏底部新增"导出整合包"按钮
- 点击后弹出文件保存对话框，默认路径为启动器根目录，默认文件名为`实例名.zip`
- 使用 miniz 库将 GameData 目录打包为 zip，包含 GameData 层级
- 排除项（仅 GameData 根目录）：
  - Squad 文件夹
  - SquadExpansion 文件夹
  - ModuleManager.ConfigCache 文件
  - ModuleManager.ConfigSHA 文件
  - ModuleManager.Physics 文件
  - ModuleManager.TechTree 文件
- 包含空文件夹
- 支持 QProgressDialog 进度条显示，可取消

## 2026-08-12/13：开关控件、导出整合包、本地化与发布清理

- **Bool 值开关控件**：新增 `ToggleSwitch`（`src/widgets/toggleswitch`），游戏设置与 Kerbals 管理的布尔项由下拉框改为开关控件。
- **导出整合包**：实例侧边栏「导出整合包」按钮，用 miniz 将 GameData 打包为 zip（排除 Squad/SquadExpansion 与 ModuleManager 缓存文件），支持进度条与取消。
- **英语本地化**：Qt 国际化框架，`tr()` 包裹全部用户可见字符串，`translations/` 维护 zh_CN / en_US 的 `.ts`/`.qm`；`main.cpp` 按 `ConfigManager::language()` 加载翻译。
- **语言切换提示重启**：切换语言后提示需重启生效。
- **发布前清理**：移除硬编码测试实例 `addTestInstanceIfEmpty()`；检查 CMake 配置完整、翻译齐备、无残留调试代码、mingw1310_64 构建通过。
- **README 与 Release Notes**：新增 `README.md`（英中双语）与 `RELEASE_NOTES.md`（英文）。